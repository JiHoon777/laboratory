import React from "react";

interface IMoveGraphContainer {}

export const MoveGraphContainer: React.FC<IMoveGraphContainer> = (props) => {
  return <div className={"w-full h-screen bg-black"}>dd</div>;
};
